import MonocartReporter from './index.js';

export default MonocartReporter;
export { MonocartReporter };

export const merge = MonocartReporter.merge;

export const startCoverage = MonocartReporter.startCoverage;
export const stopCoverage = MonocartReporter.stopCoverage;
export const takeCoverage = MonocartReporter.takeCoverage;
